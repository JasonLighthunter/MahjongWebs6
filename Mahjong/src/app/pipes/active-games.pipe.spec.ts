import { ActiveGamesPipe } from './active-games.pipe';

describe('ActiveGamesPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveGamesPipe();
    expect(pipe).toBeTruthy();
  });
});
