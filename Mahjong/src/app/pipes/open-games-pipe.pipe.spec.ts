import { OpenGamesPipe } from './open-games-pipe.pipe';

describe('OpenGamesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OpenGamesPipe();
    expect(pipe).toBeTruthy();
  });
});
